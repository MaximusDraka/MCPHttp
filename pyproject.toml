[project]
name = "mcphttp"
version = "0.1.0"
description = "MCP HTTP Server"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.4.7",
    "starlette>=0.37.0",
    "uvicorn>=0.30.0",
]
