[project]
name = "mcphttp"
version = "0.1.0"
description = "MCP HTTP Server"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.116.1",
    "mcp>=1.13.1",
    "uvicorn>=0.30.0",
]
